package ru.maximov.mathoperations;

public class power{
    public double pow(String x, String y) {
        int x1 = Integer.parseInt(x);
        int y1 = Integer.parseInt(y);
        return(Math.pow(x1,y1));
    }

}
